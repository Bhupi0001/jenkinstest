pipeline {
    agent any
    stages {
        stage ("build"){
            steps { sh 'echo sanath' }
        }
        stage ("run"){
            steps { echo 'hello' }
        }
        
    }
    
}
